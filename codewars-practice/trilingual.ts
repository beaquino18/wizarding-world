// // input is a string of three chars from the set 'D', 'F', 'I', 'K'
// // output is a single char from this set
// export function trilingualDemocracy(group: string): string {
//     let languages = group.split('');

//     languages.forEach((a, b, c) => { a === b || b === c || a === c }
        
//     });


//     /*
//     split the given string
//     compare if each string is the same (1st rule)
//     rules:
//     = 3 people, all native speakers same language = that language
//     = 3 peop;e, 2 native speaker, 1 different language = language of minority
//     = 3 people, all different languages, speak the fourth language not on the list
//     **/
    
//   }

// console.log(trilingualDemocracy('FFF'));

